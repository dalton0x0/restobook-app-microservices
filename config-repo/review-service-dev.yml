# REVIEW SERVICE - Profil DEV

# Spring
spring:
  # Database
  datasource:
    url: jdbc:mysql://localhost:3306/review_db_dev?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password:

# Review
review:
  min-comment-length: 0

# Logging
logging:
  level:
    com.restobook.reviewservice: DEBUG
